import { VerticalBox } from "std-widgets.slint";

export component SplashScreen inherits Image {

  height: 100%;
  width: 100%;
  image-fit: ImageFit.contain;
  source: @image-url("../icons/oxymon-logo.svg");
}
